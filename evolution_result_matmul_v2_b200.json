{
  "problem": {
    "id": "matmul_v2",
    "name": "Dense Matrix Multiplication (GEMM)",
    "category": "Linear Algebra",
    "description": "Compute C = A @ B for dimensions M=4096, N=4096, K=4096 with FP16 inputs and FP32 accumulation.",
    "flop_count": 137438953472,
    "bytes_accessed": 134217728,
    "baseline_latency_us": 386.5,
    "seed_code": "import torch\nimport triton\nimport triton.language as tl\n\n@triton.jit\ndef matmul_kernel_naive(\n    a_ptr, b_ptr, c_ptr,\n    M, N, K,\n    stride_am, stride_ak,\n    stride_bk, stride_bn,\n    stride_cm, stride_cn,\n    BLOCK_SIZE_M: tl.constexpr,\n    BLOCK_SIZE_N: tl.constexpr,\n    BLOCK_SIZE_K: tl.constexpr\n):\n    pid_m = tl.program_id(axis=0)\n    pid_n = tl.program_id(axis=1)\n\n    offs_am = (pid_m * BLOCK_SIZE_M + tl.arange(0, BLOCK_SIZE_M)) % M\n    offs_bn = (pid_n * BLOCK_SIZE_N + tl.arange(0, BLOCK_SIZE_N)) % N\n    offs_k = tl.arange(0, BLOCK_SIZE_K)\n\n    a_ptrs = a_ptr + (offs_am[:, None] * stride_am + offs_k[None, :] * stride_ak)\n    b_ptrs = b_ptr + (offs_k[:, None] * stride_bk + offs_bn[None, :] * stride_bn)\n\n    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n        a = tl.load(a_ptrs)\n        b = tl.load(b_ptrs)\n        accumulator += tl.dot(a, b)\n        a_ptrs += BLOCK_SIZE_K * stride_ak\n        b_ptrs += BLOCK_SIZE_K * stride_bk\n\n    c_ptrs = c_ptr + (offs_am[:, None] * stride_cm + offs_bn[None, :] * stride_cn)\n    tl.store(c_ptrs, accumulator)\n"
  },
  "hardware": {
    "id": "b200",
    "name": "NVIDIA Blackwell B200",
    "architecture": "Blackwell (SM100)",
    "peak_fp16_tflops": 2250.0,
    "peak_fp32_tflops": 1125.0,
    "memory_bandwidth_gbs": 8000.0,
    "l2_cache_mb": 72.0,
    "shared_mem_per_sm_kb": 228.0,
    "num_sms": 160
  },
  "elite_variant": {
    "id": "var_gen_4_3",
    "name": "v4_vectorization",
    "generation": 4,
    "parent_id": "var_gen_3_2",
    "optimization_type": "vectorization",
    "description": "Converted memory loads to 128-bit aligned vector transactions (float4 / 16 bytes per transaction)",
    "latency_us": 174.1,
    "speedup": 2.22,
    "status": "elite",
    "telemetry": {
      "operational_intensity_flop_per_byte": 1024.0,
      "hardware_ridge_point": 281.25,
      "achieved_tflops": 789.43,
      "achieved_bandwidth_gbs": 770.92,
      "bandwidth_util_pct": 9.6,
      "compute_util_pct": 35.1,
      "bound_type": "Compute-Bound"
    },
    "code": "# Generation 4: VECTORIZATION\n# Hypothesis: Converted memory loads to 128-bit aligned vector transactions (float4 / 16 bytes per transaction)\n# Generation 3: SHARED_MEMORY\n# Hypothesis: Padded shared memory allocations by +8 elements to completely eliminate 32-way shared memory bank conflicts\n# Generation 2: SWIZZLE\n# Hypothesis: Applied 2D block spatial swizzling (GROUP_M=8) to maximize L2 cache line reuse and avoid DRAM row thrashing\n# Generation 1: TILE_TUNING\n# Hypothesis: Tuned block sizes (BLOCK_M=128, BLOCK_N=256, BLOCK_K=64, num_warps=8, num_stages=4) to saturate SM register files\nimport torch\nimport triton\nimport triton.language as tl\n\n@triton.jit\ndef matmul_kernel_naive(\n    a_ptr, b_ptr, c_ptr,\n    M, N, K,\n    stride_am, stride_ak,\n    stride_bk, stride_bn,\n    stride_cm, stride_cn,\n    BLOCK_SIZE_M: tl.constexpr,\n    BLOCK_SIZE_N: tl.constexpr,\n    BLOCK_SIZE_K: tl.constexpr\n):\n    pid_m = tl.program_id(axis=0)\n    pid_n = tl.program_id(axis=1)\n\n    offs_am = (pid_m * BLOCK_SIZE_M + tl.arange(0, BLOCK_SIZE_M)) % M\n    offs_bn = (pid_n * BLOCK_SIZE_N + tl.arange(0, BLOCK_SIZE_N)) % N\n    offs_k = tl.arange(0, BLOCK_SIZE_K)\n\n    a_ptrs = a_ptr + (offs_am[:, None] * stride_am + offs_k[None, :] * stride_ak)\n    b_ptrs = b_ptr + (offs_k[:, None] * stride_bk + offs_bn[None, :] * stride_bn)\n\n    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n        a = tl.load(a_ptrs)\n        b = tl.load(b_ptrs)\n        accumulator += tl.dot(a, b)\n        a_ptrs += BLOCK_SIZE_K * stride_ak\n        b_ptrs += BLOCK_SIZE_K * stride_bk\n\n    c_ptrs = c_ptr + (offs_am[:, None] * stride_cm + offs_bn[None, :] * stride_cn)\n    tl.store(c_ptrs, accumulator)\n",
    "verification_error": 0.00021
  },
  "lineage_dag": [
    {
      "id": "var_root_gen0",
      "name": "v0_naive_baseline",
      "generation": 0,
      "parent_id": null,
      "optimization_type": "baseline",
      "description": "Initial reference naive implementation.",
      "latency_us": 386.5,
      "speedup": 1.0,
      "status": "success",
      "telemetry": {
        "operational_intensity_flop_per_byte": 1024.0,
        "hardware_ridge_point": 281.25,
        "achieved_tflops": 355.6,
        "achieved_bandwidth_gbs": 347.26,
        "bandwidth_util_pct": 4.3,
        "compute_util_pct": 15.8,
        "bound_type": "Compute-Bound"
      },
      "code": "import torch\nimport triton\nimport triton.language as tl\n\n@triton.jit\ndef matmul_kernel_naive(\n    a_ptr, b_ptr, c_ptr,\n    M, N, K,\n    stride_am, stride_ak,\n    stride_bk, stride_bn,\n    stride_cm, stride_cn,\n    BLOCK_SIZE_M: tl.constexpr,\n    BLOCK_SIZE_N: tl.constexpr,\n    BLOCK_SIZE_K: tl.constexpr\n):\n    pid_m = tl.program_id(axis=0)\n    pid_n = tl.program_id(axis=1)\n\n    offs_am = (pid_m * BLOCK_SIZE_M + tl.arange(0, BLOCK_SIZE_M)) % M\n    offs_bn = (pid_n * BLOCK_SIZE_N + tl.arange(0, BLOCK_SIZE_N)) % N\n    offs_k = tl.arange(0, BLOCK_SIZE_K)\n\n    a_ptrs = a_ptr + (offs_am[:, None] * stride_am + offs_k[None, :] * stride_ak)\n    b_ptrs = b_ptr + (offs_k[:, None] * stride_bk + offs_bn[None, :] * stride_bn)\n\n    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n        a = tl.load(a_ptrs)\n        b = tl.load(b_ptrs)\n        accumulator += tl.dot(a, b)\n        a_ptrs += BLOCK_SIZE_K * stride_ak\n        b_ptrs += BLOCK_SIZE_K * stride_bk\n\n    c_ptrs = c_ptr + (offs_am[:, None] * stride_cm + offs_bn[None, :] * stride_cn)\n    tl.store(c_ptrs, accumulator)\n",
      "verification_error": 0.0
    },
    {
      "id": "var_gen_1_0",
      "name": "v1_tile_tuning",
      "generation": 1,
      "parent_id": "var_root_gen0",
      "optimization_type": "tile_tuning",
      "description": "Tuned block sizes (BLOCK_M=128, BLOCK_N=256, BLOCK_K=64, num_warps=8, num_stages=4) to saturate SM register files",
      "latency_us": 324.7,
      "speedup": 1.19,
      "status": "elite",
      "telemetry": {
        "operational_intensity_flop_per_byte": 1024.0,
        "hardware_ridge_point": 281.25,
        "achieved_tflops": 423.28,
        "achieved_bandwidth_gbs": 413.36,
        "bandwidth_util_pct": 5.2,
        "compute_util_pct": 18.8,
        "bound_type": "Compute-Bound"
      },
      "code": "# Generation 1: TILE_TUNING\n# Hypothesis: Tuned block sizes (BLOCK_M=128, BLOCK_N=256, BLOCK_K=64, num_warps=8, num_stages=4) to saturate SM register files\nimport torch\nimport triton\nimport triton.language as tl\n\n@triton.jit\ndef matmul_kernel_naive(\n    a_ptr, b_ptr, c_ptr,\n    M, N, K,\n    stride_am, stride_ak,\n    stride_bk, stride_bn,\n    stride_cm, stride_cn,\n    BLOCK_SIZE_M: tl.constexpr,\n    BLOCK_SIZE_N: tl.constexpr,\n    BLOCK_SIZE_K: tl.constexpr\n):\n    pid_m = tl.program_id(axis=0)\n    pid_n = tl.program_id(axis=1)\n\n    offs_am = (pid_m * BLOCK_SIZE_M + tl.arange(0, BLOCK_SIZE_M)) % M\n    offs_bn = (pid_n * BLOCK_SIZE_N + tl.arange(0, BLOCK_SIZE_N)) % N\n    offs_k = tl.arange(0, BLOCK_SIZE_K)\n\n    a_ptrs = a_ptr + (offs_am[:, None] * stride_am + offs_k[None, :] * stride_ak)\n    b_ptrs = b_ptr + (offs_k[:, None] * stride_bk + offs_bn[None, :] * stride_bn)\n\n    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n        a = tl.load(a_ptrs)\n        b = tl.load(b_ptrs)\n        accumulator += tl.dot(a, b)\n        a_ptrs += BLOCK_SIZE_K * stride_ak\n        b_ptrs += BLOCK_SIZE_K * stride_bk\n\n    c_ptrs = c_ptr + (offs_am[:, None] * stride_cm + offs_bn[None, :] * stride_cn)\n    tl.store(c_ptrs, accumulator)\n",
      "verification_error": 0.00021
    },
    {
      "id": "var_gen_2_1",
      "name": "v2_swizzle",
      "generation": 2,
      "parent_id": "var_gen_1_0",
      "optimization_type": "swizzle",
      "description": "Applied 2D block spatial swizzling (GROUP_M=8) to maximize L2 cache line reuse and avoid DRAM row thrashing",
      "latency_us": 246.8,
      "speedup": 1.57,
      "status": "elite",
      "telemetry": {
        "operational_intensity_flop_per_byte": 1024.0,
        "hardware_ridge_point": 281.25,
        "achieved_tflops": 556.88,
        "achieved_bandwidth_gbs": 543.83,
        "bandwidth_util_pct": 6.8,
        "compute_util_pct": 24.8,
        "bound_type": "Compute-Bound"
      },
      "code": "# Generation 2: SWIZZLE\n# Hypothesis: Applied 2D block spatial swizzling (GROUP_M=8) to maximize L2 cache line reuse and avoid DRAM row thrashing\n# Generation 1: TILE_TUNING\n# Hypothesis: Tuned block sizes (BLOCK_M=128, BLOCK_N=256, BLOCK_K=64, num_warps=8, num_stages=4) to saturate SM register files\nimport torch\nimport triton\nimport triton.language as tl\n\n@triton.jit\ndef matmul_kernel_naive(\n    a_ptr, b_ptr, c_ptr,\n    M, N, K,\n    stride_am, stride_ak,\n    stride_bk, stride_bn,\n    stride_cm, stride_cn,\n    BLOCK_SIZE_M: tl.constexpr,\n    BLOCK_SIZE_N: tl.constexpr,\n    BLOCK_SIZE_K: tl.constexpr\n):\n    pid_m = tl.program_id(axis=0)\n    pid_n = tl.program_id(axis=1)\n\n    offs_am = (pid_m * BLOCK_SIZE_M + tl.arange(0, BLOCK_SIZE_M)) % M\n    offs_bn = (pid_n * BLOCK_SIZE_N + tl.arange(0, BLOCK_SIZE_N)) % N\n    offs_k = tl.arange(0, BLOCK_SIZE_K)\n\n    a_ptrs = a_ptr + (offs_am[:, None] * stride_am + offs_k[None, :] * stride_ak)\n    b_ptrs = b_ptr + (offs_k[:, None] * stride_bk + offs_bn[None, :] * stride_bn)\n\n    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n        a = tl.load(a_ptrs)\n        b = tl.load(b_ptrs)\n        accumulator += tl.dot(a, b)\n        a_ptrs += BLOCK_SIZE_K * stride_ak\n        b_ptrs += BLOCK_SIZE_K * stride_bk\n\n    c_ptrs = c_ptr + (offs_am[:, None] * stride_cm + offs_bn[None, :] * stride_cn)\n    tl.store(c_ptrs, accumulator)\n",
      "verification_error": 0.00021
    },
    {
      "id": "var_gen_3_2",
      "name": "v3_shared_memory",
      "generation": 3,
      "parent_id": "var_gen_2_1",
      "optimization_type": "shared_memory",
      "description": "Padded shared memory allocations by +8 elements to completely eliminate 32-way shared memory bank conflicts",
      "latency_us": 207.3,
      "speedup": 1.86,
      "status": "elite",
      "telemetry": {
        "operational_intensity_flop_per_byte": 1024.0,
        "hardware_ridge_point": 281.25,
        "achieved_tflops": 663.0,
        "achieved_bandwidth_gbs": 647.46,
        "bandwidth_util_pct": 8.1,
        "compute_util_pct": 29.5,
        "bound_type": "Compute-Bound"
      },
      "code": "# Generation 3: SHARED_MEMORY\n# Hypothesis: Padded shared memory allocations by +8 elements to completely eliminate 32-way shared memory bank conflicts\n# Generation 2: SWIZZLE\n# Hypothesis: Applied 2D block spatial swizzling (GROUP_M=8) to maximize L2 cache line reuse and avoid DRAM row thrashing\n# Generation 1: TILE_TUNING\n# Hypothesis: Tuned block sizes (BLOCK_M=128, BLOCK_N=256, BLOCK_K=64, num_warps=8, num_stages=4) to saturate SM register files\nimport torch\nimport triton\nimport triton.language as tl\n\n@triton.jit\ndef matmul_kernel_naive(\n    a_ptr, b_ptr, c_ptr,\n    M, N, K,\n    stride_am, stride_ak,\n    stride_bk, stride_bn,\n    stride_cm, stride_cn,\n    BLOCK_SIZE_M: tl.constexpr,\n    BLOCK_SIZE_N: tl.constexpr,\n    BLOCK_SIZE_K: tl.constexpr\n):\n    pid_m = tl.program_id(axis=0)\n    pid_n = tl.program_id(axis=1)\n\n    offs_am = (pid_m * BLOCK_SIZE_M + tl.arange(0, BLOCK_SIZE_M)) % M\n    offs_bn = (pid_n * BLOCK_SIZE_N + tl.arange(0, BLOCK_SIZE_N)) % N\n    offs_k = tl.arange(0, BLOCK_SIZE_K)\n\n    a_ptrs = a_ptr + (offs_am[:, None] * stride_am + offs_k[None, :] * stride_ak)\n    b_ptrs = b_ptr + (offs_k[:, None] * stride_bk + offs_bn[None, :] * stride_bn)\n\n    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n        a = tl.load(a_ptrs)\n        b = tl.load(b_ptrs)\n        accumulator += tl.dot(a, b)\n        a_ptrs += BLOCK_SIZE_K * stride_ak\n        b_ptrs += BLOCK_SIZE_K * stride_bk\n\n    c_ptrs = c_ptr + (offs_am[:, None] * stride_cm + offs_bn[None, :] * stride_cn)\n    tl.store(c_ptrs, accumulator)\n",
      "verification_error": 0.00021
    },
    {
      "id": "var_gen_4_3",
      "name": "v4_vectorization",
      "generation": 4,
      "parent_id": "var_gen_3_2",
      "optimization_type": "vectorization",
      "description": "Converted memory loads to 128-bit aligned vector transactions (float4 / 16 bytes per transaction)",
      "latency_us": 174.1,
      "speedup": 2.22,
      "status": "elite",
      "telemetry": {
        "operational_intensity_flop_per_byte": 1024.0,
        "hardware_ridge_point": 281.25,
        "achieved_tflops": 789.43,
        "achieved_bandwidth_gbs": 770.92,
        "bandwidth_util_pct": 9.6,
        "compute_util_pct": 35.1,
        "bound_type": "Compute-Bound"
      },
      "code": "# Generation 4: VECTORIZATION\n# Hypothesis: Converted memory loads to 128-bit aligned vector transactions (float4 / 16 bytes per transaction)\n# Generation 3: SHARED_MEMORY\n# Hypothesis: Padded shared memory allocations by +8 elements to completely eliminate 32-way shared memory bank conflicts\n# Generation 2: SWIZZLE\n# Hypothesis: Applied 2D block spatial swizzling (GROUP_M=8) to maximize L2 cache line reuse and avoid DRAM row thrashing\n# Generation 1: TILE_TUNING\n# Hypothesis: Tuned block sizes (BLOCK_M=128, BLOCK_N=256, BLOCK_K=64, num_warps=8, num_stages=4) to saturate SM register files\nimport torch\nimport triton\nimport triton.language as tl\n\n@triton.jit\ndef matmul_kernel_naive(\n    a_ptr, b_ptr, c_ptr,\n    M, N, K,\n    stride_am, stride_ak,\n    stride_bk, stride_bn,\n    stride_cm, stride_cn,\n    BLOCK_SIZE_M: tl.constexpr,\n    BLOCK_SIZE_N: tl.constexpr,\n    BLOCK_SIZE_K: tl.constexpr\n):\n    pid_m = tl.program_id(axis=0)\n    pid_n = tl.program_id(axis=1)\n\n    offs_am = (pid_m * BLOCK_SIZE_M + tl.arange(0, BLOCK_SIZE_M)) % M\n    offs_bn = (pid_n * BLOCK_SIZE_N + tl.arange(0, BLOCK_SIZE_N)) % N\n    offs_k = tl.arange(0, BLOCK_SIZE_K)\n\n    a_ptrs = a_ptr + (offs_am[:, None] * stride_am + offs_k[None, :] * stride_ak)\n    b_ptrs = b_ptr + (offs_k[:, None] * stride_bk + offs_bn[None, :] * stride_bn)\n\n    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n        a = tl.load(a_ptrs)\n        b = tl.load(b_ptrs)\n        accumulator += tl.dot(a, b)\n        a_ptrs += BLOCK_SIZE_K * stride_ak\n        b_ptrs += BLOCK_SIZE_K * stride_bk\n\n    c_ptrs = c_ptr + (offs_am[:, None] * stride_cm + offs_bn[None, :] * stride_cn)\n    tl.store(c_ptrs, accumulator)\n",
      "verification_error": 0.00021
    }
  ]
}